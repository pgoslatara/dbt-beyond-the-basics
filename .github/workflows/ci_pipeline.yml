name: ci_pipeline

on:
  pull_request:
    branches:
      - stg
      - prd
  workflow_dispatch:

env:
  DBT_CICD_RUN: "true"
  DESTINATION_BRANCH: "${{ github.event.pull_request.base.ref }}"
  DBT_DATASET: "ci_${{ github.event.number }}_${{ github.run_number }}_${{ github.sha }}"
  DBT_PROFILES_DIR: "."
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  UV_VERSION: "0.9.24"

jobs:
  ci_pipeline:
    permissions:
      pull-requests: write
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          version: ${{ env.UV_VERSION }}

      - name: "Set up Python"
        run: uv python install

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v3'

      - name: Save stg GCP credentials to runner
        run: |
          echo "${{ secrets.DBT_CICD_SA_SECRET_KEY_STG_BASE64_ENCODED }}" | base64 --decode >> ./service_account.json && \
          export GOOGLE_APPLICATION_CREDENTIALS="$(pwd)/service_account.json"
        if: ${{ github.event.pull_request.base.ref == 'stg' }}

      - name: Save prd GCP credentials to runner
        run: |
          echo "${{ secrets.DBT_CICD_SA_SECRET_KEY_PRD_BASE64_ENCODED }}" | base64 --decode >> ./service_account.json && \
          export GOOGLE_APPLICATION_CREDENTIALS="$(pwd)/service_account.json"
        if: ${{ github.event.pull_request.base.ref == 'prd' }}

      - name: Authenticate with GCP
        run: |
          gcloud auth activate-service-account dbt-cicd@beyond-basics-$DESTINATION_BRANCH.iam.gserviceaccount.com --key-file=./service_account.json && \
          gcloud config set project beyond-basics-$DESTINATION_BRANCH

      - name: Install python packages
        run: uv sync

      - name: dbt dep
        run: uv run dbt deps

      - name: dbt parse
        run: uv run dbt parse --target $DESTINATION_BRANCH

      - name: dbt-bouncer (manifest checks)
        run: uv run dbt-bouncer --only manifest_checks

      - run: uv run pytest ./tests/pytest -m no_deps -n 5

      - run: uv run dbt deps

      - name: dbt debug
        run: uv run dbt debug --target $DESTINATION_BRANCH

      # Create objects in BigQuery and run unit tests
      - name: dbt seed
        run: uv run dbt seed --target $DESTINATION_BRANCH

      - name: dbt run --empty
        run : uv run dbt run --empty --target $DESTINATION_BRANCH

      - name: dbt-bouncer (run_results checks)
        run: uv run dbt-bouncer --only run_results_checks

      - name: Run unit tests
        run: uv run dbt test --select "test_type:unit" --target $DESTINATION_BRANCH

      # Needs to be run early in the CI pipeline to allow `dbt docs generate` to succeed (which is a dependency of pre-commit and dbt-coverage)
      - name: dbt build
        run: uv run dbt --warn-error build --fail-fast --full-refresh --exclude "test_type:unit" --target $DESTINATION_BRANCH

      - name: dbt docs generate
        run: uv run dbt docs generate --target $DESTINATION_BRANCH

      - name: dbt-bouncer (catalog checks)
        run: uv run dbt-bouncer --only catalog_checks

      - name: pre-commit run -a
        run: SKIP=autoflake,dbt-compile,dbt-docs-generate uv run pre-commit run -a

      - name: Generate docs coverage report
        id: dbt-docs-coverage-report
        run: |
          uv run dbt-coverage compute doc --cov-report coverage-doc.json --output-format markdown >> coverage-doc.md
          sed -i '/# Coverage report/c\# Doc coverage report.' coverage-doc.md

      - uses: mshick/add-pr-comment@v3
        with:
            message-path: coverage-doc.md
            message-id: dbt-docs-coverage-report
            refresh-message-position: true

      - name: Generate test coverage report
        id: dbt-test-coverage-report
        run: |
          uv run dbt-coverage compute test --cov-report coverage-test.json --output-format markdown >> coverage-test.md
          sed -i '/# Coverage report/c\# Test coverage report.' coverage-test.md

      - uses: mshick/add-pr-comment@v3
        with:
            message-path: coverage-test.md
            message-id: dbt-test-coverage-report
            refresh-message-position: true

      - run: uv run pytest ./tests/pytest -m run_results_json -n 5

      - name: dbt source freshness
        run: uv run dbt source freshness --target $DESTINATION_BRANCH || true # source freshness is allowed to fail in CI due to stale sources, the next command runs pytest on sources.json to validate the generated SQL (the prupose of this step)

      - run: uv run pytest ./tests/pytest -m sources_json -n 5

      - name: dbt build incremental models
        run: uv run dbt --warn-error build --fail-fast --select config.materialized:incremental --exclude "test_type:unit" --target $DESTINATION_BRANCH

      # Mart Monitor
      - run: if [ "$DESTINATION_BRANCH" == "stg" ]; then DBT_CICD_RUN="false" uv run dbt compile --target $DESTINATION_BRANCH --threads 64; else echo "Only runs for PRs to stg"; fi # Need manifest.json as exists during $DESTINATION_BRANCH runs and not during CI runs

      - run: if [ "$DESTINATION_BRANCH" == "stg" ]; then uv run python ./scripts/mart_monitor_commenter.py --dbt_dataset $DBT_DATASET --pull_request_id ${{ github.event.number }} --target_branch $DESTINATION_BRANCH; else echo "Only runs for PRs to stg"; fi

      - name: Ensure ERD can be generated
        run: uv run python ./scripts/generate_marts_erd_diagram.py

  dev_container:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6

      - name: Build container and run dbt command
        uses: devcontainers/ci@v0.3
        env:
          DBT_DATASET: "cicd_${{ github.event.number }}_${{ github.run_number }}_${{ github.sha }}_dev_container"
          DBT_PROFILES_DIR: "."
          DESTINATION_BRANCH: "${{ github.event.pull_request.base.ref }}"
        with:
          runCmd: |
            echo "${{ secrets.DBT_CICD_SA_SECRET_KEY_STG_BASE64_ENCODED }}" | base64 --decode >> ./service_account.json
            export GOOGLE_APPLICATION_CREDENTIALS="$(pwd)/service_account.json"cloud auth activate-service-account dbt-cicd@beyond-basics-$DESTINATION_BRANCH.iam.gserviceaccount.com --key-file=./service_account.json
            gcloud config set project beyond-basics-$DESTINATION_BRANCH
            uv sync
            uv run dbt deps
            uv run dbt compile --target $DESTINATION_BRANCH
          env: |
            DBT_DATASET
            DBT_PROFILES_DIR
            DESTINATION_BRANCH

  docker:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
            images: dbt-beyond-the-basics
            tags: |
                type=ref,event=branch
                type=raw,value=${{ github.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Determine python version
        id: python-version
        shell: bash
        run: |
            export PYTHON_VERSION=$(cat ./.python-version)
            echo "PYTHON_VERSION: $PYTHON_VERSION"
            echo "PYTHON_VERSION=$PYTHON_VERSION" >> $GITHUB_OUTPUT

      - name: Build Docker image
        uses: docker/build-push-action@v6
        with:
            build-args: |
                "PYTHON_VERSION=${{ steps.python-version.outputs.PYTHON_VERSION }}"
            cache-from: type=gha
            cache-to: type=gha,mode=max
            context: .
            file: ./Dockerfile
            load: true
            push: false
            tags: ${{ steps.meta.outputs.tags }}

      - name: Test Docker image
        run: |
            docker run --rm \
              --env DBT_DATASET="test" \
              --volume ~/.config/gcloud:/root/.config/gcloud \
              dbt-beyond-the-basics:${{ github.sha }} \
              dbt parse
