version: 2

models:
  - name: stg_public_datasets__bitcoin_blocks
    description: Stuffing to get dbt-bouncer to pass.
    columns:
      - name: block_hash
        tests:
          - unique
          - not_null
